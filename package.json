{
  "type": "module",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "format": "npm run format --prefix ./server && npm run format --prefix ./client",
    "start": "yarn workspace intelligent-crawler-server start"
  },
  "dependencies": {
    "node-html-parser": "^7.0.1"
  }
}
